# `src/events/publishers/`

Event publishing helpers.

Put here:
- Event builders (ensure `tenant_id`, `event_id`, `correlation_id`, `aggregate_id/version`)
- EventBridge publish client wrappers
- Schema validation before publish


