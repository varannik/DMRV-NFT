# `src/services/`

Core business logic (domain).

Put here:
- Methods that implement the use-cases of the service
- Domain validations and state transitions
- Event emission boundaries (publish after commit, use outbox if needed)


