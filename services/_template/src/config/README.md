# `src/config/`

Configuration loading and validation.

Put here:
- Environment variable schema + validation (fail fast on boot)
- AWS client configuration (region, endpoints for local dev)
- Feature flags and per-environment toggles


