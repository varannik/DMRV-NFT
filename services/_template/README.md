# `services/_template/`

Reference skeleton for new services.

Create a new service by copying this template into `services/<service-name>/` and then:
- rename placeholders
- wire up event subscriptions
- add migrations (if stateful)
- add unit/integration tests


