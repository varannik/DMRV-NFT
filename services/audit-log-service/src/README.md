# `services/audit-log-service/src/`

Audit log service implementation.

Put here:
- Consumers for all events (or routed subsets)
- Append-only storage logic and integrity checks
- Export/report endpoints (admin-only)


