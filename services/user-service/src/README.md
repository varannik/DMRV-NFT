# `services/user-service/src/`

User service implementation.

Put here:
- Auth flows (login/refresh/logout) if not handled by gateway
- Role and permission management
- SCIM/SAML/OIDC integration adapters


