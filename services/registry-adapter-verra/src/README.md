# `services/registry-adapter-verra/src/`

Verra adapter implementation.

Put here:
- API client wrapper with retries/circuit breaker
- Mapping layer from canonical payload → Verra schema
- Webhook/polling handlers (depending on registry capabilities)


