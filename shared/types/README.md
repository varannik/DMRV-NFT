# `shared/types/`

Shared TypeScript types across services.

Put here:
- MRV payload types (raw/verified/canonical forms)
- Credit lifecycle states and registry identifiers
- Pagination, error, and API response types


