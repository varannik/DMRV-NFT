# `shared/events/`

Event envelope and schema tooling.

Put here:
- Standard event envelope types (`event_id`, `event_type`, `tenant_id`, `correlation_id`, etc.)
- JSON Schema definitions and validators
- Helpers for versioning/migration (v1 → v2 dual-publish support)


