# `shared/errors/`

Standard error types and mapping.

Put here:
- Error codes and user-safe messages
- Mapping from domain errors → HTTP errors
- Retryable vs non-retryable error classification (important for consumers)


