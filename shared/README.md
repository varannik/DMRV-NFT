# `shared/`

Shared libraries used by multiple services.

This folder is critical to enforce consistency across the platform:
- Auth and tenant context primitives
- Event envelope and schema validation
- Error formats and logging/tracing
- Shared types for MRV payloads and credit lifecycle


