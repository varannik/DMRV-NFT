# `docs/ci-cd/`

CI/CD design and release management.

Put here:
- Pipeline stages (lint/test/build/scan/deploy)
- Environment promotion (dev → staging → prod) and approvals
- Deployment strategies (rolling/canary/blue-green) and rollback triggers
- SBOM/provenance generation and artifact retention
- Terraform workflow (plan/apply), drift detection, and secrets handling


