# `infrastructure/terraform/eventbridge/`

EventBridge custom bus, schemas, rules, and targets.

Put here:
- Event bus definitions (e.g., `dmrv-platform`)
- Rule patterns per event type/domain
- Targets to SQS/Lambda/Step Functions (as needed)
- DLQ wiring + alarms


