# `infrastructure/terraform/modules/`

Reusable Terraform modules.

Guidelines:
- Each module should be environment-agnostic and parameterized.
- Keep outputs stable; add new outputs rather than renaming.
- Document IAM permissions explicitly (what/why).


