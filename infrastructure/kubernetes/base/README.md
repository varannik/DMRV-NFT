# `infrastructure/kubernetes/base/`

Base Kubernetes resources shared by all environments:
- Namespaces
- Common RBAC
- Shared ConfigMaps (non-secret)
- Base Deployments/Services patterns (labels, probes, resource requests/limits)


