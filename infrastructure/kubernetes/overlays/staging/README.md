# `infrastructure/kubernetes/overlays/staging/`

Staging environment overlays.

Purpose:
- Mirrors production as closely as possible
- Used for integration tests (registries sandbox, NEAR testnet), perf tests, and release candidates


