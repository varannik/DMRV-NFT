# `tests/load-tests/`

Performance and load testing.

Put here:
- API load tests (p50/p95/p99, error rate targets)
- Event throughput tests (EventBridge → SQS → consumers)
- Soak tests (hours/days) to catch memory leaks and lag accumulation


